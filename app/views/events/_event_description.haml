- if event.title.length > 35
  .event_with_more_than_one_line_title
    = link_to(event.title, event.link)
- if event.title.length <= 35
  .event_with_one_line_title
    = link_to(event.title, event.link)
